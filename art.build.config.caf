npm:
  description: "" Art.Nano.Engine
  dependencies: {}

webpack:
  # common properties are merged into each target's properties
  common:
    mode: "development"
    # devServer:
      # start with: lt -s imikimi --port 8080
      # public: :witty-liger-27.localtunnel.me

  # each target's individual properties
  targets:
    perf: {}
    test: {}